# Snyk (https://snyk.io) policy file
---
exclude:
  # Puedes usar `global` o `code`. `code` aplica solo a Snyk Code.
  code:
    # Ignora carpetas que empiezan con test_ en cualquier nivel
    - "**/test_*/**"

    # Ignora archivos que empiezan con test_
    - "**/test_*.*"

    # (Opcional, recomendado) patrones comunes de tests
    - "**/__tests__/**"
    - "**/*.spec.*"
    - "**/*.test.*"